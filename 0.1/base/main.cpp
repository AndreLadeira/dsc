#include <iostream>
#include <ctime>
#include <iomanip>

using namespace std;

int main()
{
//    cout<<"Starting...\n";

//    clock_t begin = clock();

//    randomize(a);
//    cout<< "Elapsed time: " << setprecision(3) <<
//           (clock() - begin) / static_cast<double>(CLOCKS_PER_SEC) << endl;

//    begin = clock();
//    randomize(b);
//    cout<< "Elapsed time: " << setprecision(3) <<
//           (clock() - begin) / static_cast<double>(CLOCKS_PER_SEC) << endl;
}
